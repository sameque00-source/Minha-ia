# Gerado a partir do MASTER por `minhaia sync` (fonte única = MASTER, nunca versionar cópia)
/vendor/
/.claude/agents/
/.claude/skills/
/.sync-staging/
*.sync-old/

# Estado de runtime (memória, missões, logs, telemetria)
/data/
*.log

# Segredos — nunca versionar
/.secrets/
.env
.env.*
*.pem
*.key

node_modules/
.DS_Store
